export const LIMIT_PER_PAGE = 16;

export const DEFAULT_PAGE = 1;

export enum SearchParamsKeys {
  page = 'page',
  search = 'search',
  limit = 'limit',
  offset = 'offset',
}

export const LIMIT_TO_START_SEARCH = 3;

export const NO_LIMIT_SEARCH = 100000;
